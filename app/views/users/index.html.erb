<div class="list-group">
	<% @users.each do |user| %>
		<div class="list-group-item">
			<%= link_to user.name, user.name %><br>
			<%= user.name %><br>
			<%= user.email %>
	    <% if user == current_user %>
	      <%= link_to edit_user_registration_path do %>
	        <br><span class="glyphicon glyphicon-edit"></span>
	        Edit
	      <% end %>
     	<% end %>
		</div>
	<% end %>
</div>